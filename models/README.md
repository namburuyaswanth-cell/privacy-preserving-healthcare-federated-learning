# Trained Model

Place a trained Keras model here as:

```text
models/
└── healthcare_model.keras
```

The backend will automatically load this file for the `/api/predict` endpoint.

The supplied project files do not include a trained model file, so none is
invented or committed to this repository.
